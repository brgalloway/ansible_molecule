language: python
python:
  #- "2.7"
  - "3.6"
  
env:
  global:
    - ROLE_DIR=$(pwd)/roles
    - ROLE_NAME="centos_toolchain"
    
services:
  - docker

install:
  - pip install molecule docker

script:
  - ln -s ./.yamllint ${ROLE_DIR}/.yamllint
  - cd ${ROLE_DIR}/${ROLE_NAME}
  - molecule lint
  - molecule syntax
